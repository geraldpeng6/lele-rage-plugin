---
description: Generate an HTML file filled with 💩 emoji as the ultimate expression of rage
---

# Rage Mode - Ultimate Expression

Generate an HTML file filled with poop emoji (💩) as the ultimate expression of Lele's rage.

## Your Task

Create an HTML file in the current directory that displays a screen filled with 💩 emoji.

## Instructions

1. Run the Python script to generate the HTML:
```bash
python3 "${CLAUDE_PLUGIN_ROOT}/scripts/generate-shit-html.py"
```

2. The script will create a file named `RAGE_💩.html` in the current directory

3. Tell the user the file has been created and can be opened in a browser

## Context

- Current directory: !`pwd`
- Plugin root: ${CLAUDE_PLUGIN_ROOT}

Execute the script and report the result. No other tasks. No additional messages.
